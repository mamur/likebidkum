<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jasakreatif.bidkum"
    android:versionCode="2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logodki"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- <meta-data -->
            <!-- android:name="android.app.default.searchable" -->
            <!-- android:resource="SearchableActivity" /> -->

        </activity>
        <activity android:name=".TambahTeman" />

        <!-- <activity android:name=".WriteSuggestion" -->
        <!-- android:label="Write Suggestion"> -->
        <!-- <action android:nabidkumggestion"/> -->
        <!-- <category android:name="android.intent.category.DEFAULT"/> -->


        <!-- </activity> -->


        <!-- <activity android:nabidkumivity" -->
        <!-- android:label="@string/title_activity_searchable" -->
        <!-- android:launchMode="singleTop" -->
        <!-- android:windowSoftInputMode="adjustPan|adjustNothing" -->
        <!-- tools:ignore="DuplicateActivity"> -->
        <!-- <intent-filter> -->
        <!-- <Action android:name="android.intent.action.SEARCH"></Action> -->
        <!-- </intent-filter> -->
        <!-- <meta-data android:name="android.app.searchable" -->
        <!-- android:resource="@xml/searchable" /> -->
        <!-- </activity> -->

        <activity android:name=".PageLauncherActivity" />
        <activity android:name=".PersyaratanActivity" />
        <activity android:name=".PanduanActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".MainExternalActivity" />
        <activity android:name=".UserLoginActivity" />
        <activity android:name=".internal.PendampinganActivity" />
        <activity android:name=".internal.SaranPendapatHukumActivity" />
        <activity android:name=".internal.AdvokasiActivity" />
        <activity android:name=".internal.PembuatanSopMouActivity" />
        <activity android:name=".internal.KumpulanPeraturanActivity" />
        <activity android:name=".external.MasyarakatActivity" />
        <activity android:name=".external.LembagaActivity" />
        <activity android:name=".external.DashboardDesignActivity" />
        <activity android:name=".internal.DashboardDesignActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".UserLoginActivity3" />
        <activity android:name=".DiskusiActivity" />
        <activity android:name=".chatroom"></activity>
    </application>

</manifest>